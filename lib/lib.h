#include<iostream>
#include <cmath>
using namespace std;

#ifndef LIB_H
#define LIB_H



#define MIN(x, y) ((x) < (y) ? (x) : (y))
#define MAX(x, y) ((x) > (y) ? (x) : (y))
#define SQUARE(x) ((x) * (x))
#define POWER(x, n) (int)pow((x), (n))
#define IS_EVEN(x) ((x) % 2 == 0)
#define IS_ODD(x) ((x) % 2 != 0)

#endif 